name: job-search-digest

on:
  schedule:
    # 00:07 UTC = 8:07pm ET (previous evening). Adjust to taste.
    - cron: "7 0 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: job-search-digest
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "git hub"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: "git hub/requirements.txt"

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Generate config.yaml from example + secret
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -z "$DISCORD_WEBHOOK_URL" ]; then
            echo "::error::DISCORD_WEBHOOK_URL secret is not set"
            exit 1
          fi
          cp config.example.yaml config.yaml
          python -c "
          import os, re
          p = 'config.yaml'
          s = open(p).read()
          s = re.sub(r'webhook_url:\s*\"\"', 'webhook_url: \"' + os.environ['DISCORD_WEBHOOK_URL'] + '\"', s)
          open(p, 'w').write(s)
          "

      - name: Post digest
        run: python -m bot.digest

      - name: Commit follow-up marks
        run: |
          if [ ! -f seen_jobs.db ]; then
            exit 0
          fi
          cd ..
          git config user.name  "job-search-bot"
          git config user.email "job-search-bot@users.noreply.github.com"
          git add "git hub/seen_jobs.db"
          if git diff --staged --quiet; then
            echo "no follow-up changes"
            exit 0
          fi
          git commit -m "Digest: mark follow-ups sent [skip ci]"
          git push
